import { Mutation } from "fastman/mutation";
import { IModel } from "../types";

// // 开发者可在这里将需要的mutation变异函数暴露为action
// declare module "fastman/bootstrap" {
//   interface IMutation<T> {
//     mutationName(data?: any);
//   }
// }

// export const mutationName: Mutation<IModel> = (model, data, actions, error) => {
//   return {
//     name: "Fastman V2"
//   };
// };
