import { IProps } from "fastman/view";

// Model数据类型
export interface IModel {
  name: string;
}

// // 如需要,应该在这里定义子组件的属性类型
// export interface IComponentProps extends IProps<IModel> {
//   foo: string;
//   bar: number;
// }
